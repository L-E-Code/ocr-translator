services:
  ocr-translator:
    build:
      context: .
      dockerfile: docker/Dockerfile
    volumes:
      - ./app:/app/app
      - easyocr-cache:/root/.EasyOCR
    environment:
      - PYTHONUNBUFFERED=1

volumes:
  easyocr-cache:
